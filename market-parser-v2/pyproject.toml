[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "market-parser-v2"
version = "0.1.0"
description = "Clean skeleton for provider-aware marketplace parser V2"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
market-parser-v2 = "market_parser_v2.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
